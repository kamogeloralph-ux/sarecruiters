name: Sync Government vacancies
on:
  schedule:
    # Government public-service circulars are normally published weekly; run Friday afternoon SAST.
    - cron: "30 13 * * 5"
  workflow_dispatch:
    inputs:
      years:
        description: "Comma-separated publication years to scan"
        required: false
        default: "2026"
        type: string
      max_circular:
        description: "Highest circular number to inspect per year"
        required: false
        default: "60"
        type: string
permissions:
  contents: read
concurrency:
  group: government-vacancy-sync
  cancel-in-progress: false
jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      GOVERNMENT_MAX_CIRCULAR: ${{ inputs.max_circular || '60' }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - name: Install dependencies
        run: npm install
      - name: Sync all Government vacancies from DPSA circulars
        run: GOVERNMENT_YEARS="${{ inputs.years || '' }}" npm run sync:government
